# 服務器配置
server.address=0.0.0.0
server.port=8080

# 應用名稱
spring.application.name=Qrcode

# PostgreSQL 資料庫配置
spring.datasource.url=jdbc:postgresql://scan-shop-qrcode.com:5432/qrcode
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=sa
spring.datasource.password=gaojunting11
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate 配置
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL 初始化
spring.sql.init.mode=always

# Email 配置
spring.mail.default-encoding=UTF-8
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=g0909095118@gmail.com
spring.mail.password=aden yisg jtbq ekqy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# 應用配置
app.base-url=http://scan-shop-qrcode.com/qrcode/provider

# 連接池配置（建議添加）
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000

# 請求超時配置（建議添加）
spring.mvc.async.request-timeout=300000

spring.jackson.time-zone=Asia/Taipei
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Taipei

# 文件上傳配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
## 啟用 H2 控制台
#spring.h2.console.enabled=true